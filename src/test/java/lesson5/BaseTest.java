package lesson5;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Test;

import java.util.concurrent.TimeUnit;

public class BaseTest {

 /*   WebDriver driver;

    @BeforeTest

    public void init () {
        driver = new ChromeDriver();

        //Неявные ожидания
        driver.manage().timeouts().pageLoadTimeout(30, TimeUnit.SECONDS);           //Работает только для driver.get!!
        driver.get(Url);
        driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);            //Ожидание всех елементов

        //Явные ожидания
        WebDriver wait = new WebDriverWait(driver, 10)
        wait.until              //продолжи....

    }

    @AfterTest

    public void stop () {
        driver.quit();
*/
    }

